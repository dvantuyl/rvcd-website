---
layout: contrast-container
---

<div class="lg:px-10 py-5 border-t-4 border-blush-700 border-opacity-10 border-dashed">
  <h3 class="font-subtitle font-semibold tracking-wider text-3xl font-bold leading-tight text-blush-700">{{title}}</h3>
  <time class="text-gray-800 text-xl font-semibold mb-5 block" datetime="{{ date | dateToFormat('yyyy-LL-dd') }}">{{ date | dateToFormat('dd LLL yyyy') }}</time>
  <article class="text-gray-700 prose prose-xl font-default font-semibold">
    {{content | safe}}
  </article>
</div>